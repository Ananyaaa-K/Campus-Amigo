version: '3'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./prisma/dev.db:/app/prisma/dev.db # Persist SQLite database
    environment:
      - DATABASE_URL=file:./dev.db
